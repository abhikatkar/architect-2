/**
 * Facts shared by /privacy and /terms.
 *
 * One constant each, because two pages carrying their own copy of a date is
 * exactly how they end up disagreeing. They were written as "27 September
 * 2026" in both files and both were wrong.
 */
export const LEGAL_UPDATED = "26 September 2026";

/** Where a deletion request goes. */
export const LEGAL_CONTACT = "katkarabhi91@gmail.com";
